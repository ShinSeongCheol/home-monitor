import {DashboardWidget} from "../../../widgets/dashboard";

export const DashboardPage = () => {
    return (
        <main className={'w-full h-full'}>
            <DashboardWidget />
        </main>
    );
}
